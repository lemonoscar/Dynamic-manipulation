# Step1700：仅由60秒仿真时钟结束的真实物理测试

本次已正常跑满60.00秒仿真时间，连续3000个控制步、150次真实RGB模型查询。终止原因为simulation_clock_60s；没有达到查询次数、墙钟、关节阈值、PCT端点、零命令或任务完成门而提前结束。完整搬运未成功。

## 修改与身份

分支feat/train-seed-fit-diagnostic-20260908，实际执行提交920ccc5b74e097ddf83dfc9ee0c1f039098684ef。新增timer-only研究模式：运行阈值只记录；PCT端点停止门显式禁用；查询次数与父类控制步watchdog不参与退出；每个物理步前统一检查真实仿真timestamp；到60秒前不执行第3001步。零命令继续物理步，动作/数据请求异常取消当前待执行动作并保持本episode可信目标后重新查询。评分、录像或object证据异常不会重复物理步，受影响评分锁存unknown。

仍保留最终机械位置、速度和夹爪命令裁剪；无效/非有限输入不伪造为有效动作。初始化身份、真实相机与源状态校验在计时开始前完成。旧评测模式与原0.10m snap、0.12m位置、0.14rad朝向、0.5%饱和统计身份保留；本模式所有统计门不用于结束episode。独立实际方法CPU测试6项、已有导航/录制回归11项通过，真实60秒运行进一步验证计时接线。

模型为原step1700 full-model best.pt，SHA256 474d03ff22da939f763574bf434c75226a2a2533ae984ced80e25e34a1df7fa0；原normalizer与causal_command_5hz不变。训练种子16100000，源tick80/1.6秒恢复一次；IsaacSim5.1新物理条件，不是Sim6 solver还原。RTC关闭，不使用depth，不重启训练。

## 实际任务结果

模型在21.2秒声明源导航完成，进入PICK；31.8秒声明PICK完成，进入NAV_TO_TARGET。到60秒仍未进入PLACE。任务历史明确为未经独立完成反馈确认的MODEL_ADVANCE_CLAIM，evaluator没有驱动planner。

独立审核显示PICK阶段TCP最近仍距物体0.2183m，该时刻主夹爪目标为0.04m（全开）；全程物体最大位移约0.300mm，正向抬升为0。物体没有被抬起，未建立抓取或携物证据；模型在尚未抓起物体时自行切换到了去放置位置。正常跑满计时不构成完整任务成功。严格接触缺传感证据，仍为unknown；本次不能宣称RTC改善或完整模型能力通过。独立物体位姿、TCP距离、夹爪与控制记录审核见TIMER60_AUDIT.md/json。

原限幅统计率13.1868%，原0.5%门失败，作为结果保留，没有中断运行。物理profile无抓取约束、无中途物体重置，仍声明manipulation base/support locks，不标为纯物理成功。

## 资源与工件

H20 GPU2推理、GPU3仿真，单次尝试。物理进程941724从2026-09-08 23:21:25至23:32:25 CST，墙钟659.897秒（含物理初始化）；其中60秒仿真包含初始相机历史等待。模型服务940379于23:32:26自动关闭，GPU2/3已释放。没有额外墙钟截止导致本次结束。

远端运行目录：/diff/wallx_workspace/dzb/integration_runs/train_seed_fit_20260908_timer60_v1。results/含原始trace、summary、命令、身份与300帧录像；视频另记录9帧overview缺失，不能隐去。传输包SHA256为0f42ce7e6a0bc48b5e256800843e03e22b0635ee85dc5cb546e3411e839fe030，发送与接收核验相同。

ACTIVE_CONTRACT.json为本次生效的停止/恢复合同；manifest.json保留父实验历史字段，其timer60块明确本次覆盖项。原始数据和旧实验工件未覆盖。
