# 2026-09-05 开发快照发布记录

范围：在当前 `Manipulation_Navi_v1` 分支整理已有实现、新开发文档和工件排除规则，并发布代码。
仓库属于研究平台，兼有机器人仿真栈；本次不将训练完成表述为模型验收通过。
起始 HEAD 为 `134cf61fc13b0eca5c9c985812dece556c2ac1ca`，开始时已有训练、评估、感知及测试的未提交改动。
已检查配置、实际构建和执行入口、正式训练报告/日志、完整开环报告、运行中闭环汇总、Git 跟踪文件与来源记录。

## 变更和兼容性

| 范围 | 发布内容 | 兼容性 |
|---|---|---|
| 模型/数据/训练 | ABot-M0 双 DiT、固定源 5 Hz 数据适配、初始化与分阶段训练 | 使用新 5 Hz model/data schema；旧 0.04s 或 Waypoint checkpoint 不混用 |
| 评估/服务/执行 | 最终模型严格绑定、开环聚类区间、迁移闭环双条件、失败证据 | saturation 门槛仍为 0.005；正在运行的源码文件内容保持原样 |
| 感知诊断 | LiDAR、SAM2 探针与本机查看器，SPCGVLA 草案 | 独立工具，未接入正式模型输入 |
| 文档 | 新开发指南、架构分析、实验卡和贡献规则；修正 README/索引/历史范围 | 原训练报告和历史合同保留 |
| 工件 | 扩展 `.gitignore`，移除索引中的旧 `policy.pt`，更新资源锁和可选权重测试 | 本地权重保留，runtime 仍校验独立合同 SHA |

路径映射：源码和旧文档无搬迁；外部 `evaluation/ARCHITECTURE_ANALYSIS.md` 整理为
`docs/joint_trajectory_architecture_analysis_20260905.md`，外部训练/评估报告提炼为
`docs/experiments/formal_5hz_20260905.{md,summary.json}`。原始文件未覆盖。
`assets/policies/go2_x5_pct_dog_only/policy.pt` 路径仍可本地安装，发布树仅保留其 contract 和来源。
已有 Git 历史中的权重对象不因移除索引消失，本次不改写历史。

## 仓库可维护性检查

RIGOR 人工检查为 **65/100**，仅评价本次可核验的开发流程，不是模型质量评分。
未获取的远端保护或发布配置不按已实施计分。

| 维度 | 分数 /20 | 证据及缺口 |
|---|---:|---|
| 可复现发布 | 14 | 模型/数据/源码/报告哈希及 resolved 配置齐备；依赖精确锁和通用工件迁移尚不完整 |
| 接口与架构 | 16 | schema、状态边界、模型/执行测试明确；formal loader 仍依赖训练脚本构建器 |
| 变更治理 | 8 | 合同与回归可核验；远端保护、指定 owner 和自动 CI 门禁未确认 |
| 历史与分支 | 10 | 历史合同与训练 dirty patch 保留；多个研究分支的支持周期尚未定义 |
| 文档与操作 | 17 | README、开发指南、实验卡和历史入口连通；真实外部依赖的全新机器复现未执行 |

| 严重度 | 证据与影响 | 最小处置 | 置信度 |
|---|---|---|---|
| high，已处理 | HEAD 跟踪 `policy.pt`；违背本次排除权重的发布范围 | 仅移除索引，保留合同/本地文件；无权重导出检查 | 高 |
| high，已处理 | README/索引仍引用早期未训练、0.04s 合同；新开发者会选错入口 | 更新当前 5 Hz 入口，历史页加范围说明 | 高 |
| high，仍存在 | `resolved_run.json` 绑定绝对数据路径，闭环启动器固定本机资源路径 | 明确安装布局；下一轮独立实现路径迁移与配置化 | 高 |
| medium，仍存在 | 训练/仿真依赖环境分离，但 Python 包约束不是完整 lockfile | 后续记录可安装的精确环境与外部资源获取清单 | 高 |
| medium，未核实 | 远端 branch protection、维护责任分工、自动状态检查 | 维护方后续核实并配置；不宣称本次已启用 | 中 |

目录与依赖方向、仿真/真机边界见[开发指南](joint_trajectory_development_guide.md)。
既有 `src/`、`scripts/`、`configs/`、`tests/`、`docs/`、`assets/` 布局保留；
模型只接收部署合同内观测，评估真值由独立物理证据层消费，工件放在 Git 外。

## 实施批次与回退

1. 核验已有实现及训练/评估证据，执行完整回归。发布过程中不更改正在运行的 `src/`、`scripts/` 源文件内容。
2. 新建文档并修正导航，提取去除机器路径的小型报告快照；原始结果不写回。回退可 revert 文档提交，不触碰运行目录。
3. 排除工件、更新资源锁，验证暂存树与无权重导出，再提交并非强制推送当前分支。
   若需回退工件策略，仍从本地授权副本安装，不重新发布权重。停止发布不删除本地证据或终止评估进程。

本次沿用包版本 `0.1.0`，以 Git commit 标识开发快照；不创建性能通过 tag。
后续改动从独立功能分支开始，跨合同 backport 单独审查，维护者确认模型/时钟/归一化/物理辅助等关键路径。
不合并其他模型分支，不移动历史 tag，不强推。具体贡献和工件规则见 [CONTRIBUTING](../CONTRIBUTING.md)。

## 验证记录和后续

在已有完整依赖环境执行 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 OMP_NUM_THREADS=2 python -m pytest -p no:cacheprovider`：
**561 passed**。两项 HTTP 测试需要允许本机回环 socket；初次沙箱失败已在允许该通信后复核。
保留一项已有 pytest 参数化生成器的未来兼容弃用警告，不影响当前测试通过。
从暂存树导出不含模型、数据和 `artifacts/` 的副本，完整回归 **560 passed, 1 skipped**；
跳过项为未安装外部 locomotion 权重的实际文件完整性检查。
另用仅含 NumPy/pytest 及其依赖、不含 Torch/Transformers/Pillow/Accelerate 的环境执行 README 四组检查，
得到 **27 passed, 1 skipped**。导出测试复用了已安装的 Python 依赖，未测试从互联网全新下载依赖。
文档本地链接、暂存树空白检查、工件后缀/目录、凭据模式和新增 blob 大小检查通过；
本次新建或修改文件无二进制，最大约 90KB。原本已跟踪的机器人几何资源保留。
冻结闭环 runner 源码摘要仍为 `1fd7bfb2f104bebf3260b0461549e6ae37ce6e2716476b8e692c19c15ed5ad84`，
与原 manifest 一致。

本次没有重新训练、启动另一批 300 次评估或执行真机动作。正在运行的闭环结果保持独立，
完成后应新增带完整分母、95% 区间和失败归因的最终实验摘要。下一项低影响维护工作是只读复核该报告是否 complete。
