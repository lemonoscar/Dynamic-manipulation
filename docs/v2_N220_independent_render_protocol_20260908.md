# V2 N220 independent-render protocol amendment

Master authorized this bounded amendment on 2026-09-08 after v4: one old-tail model trajectory completed; two attempted comparison branches rejected solely because their independently rendered JPEG hashes differed. Source/model/code identity and all first-query physical state values were identical across the three attempts. Rejected RGB images were not persisted, so neither the magnitude nor the cause of their differences is established. The prior failures remain unchanged.

This amendment allows exactly two further branch attempts, replacement and VJP RTC, each <=900 seconds. Both adopt immutable first_plan.json SHA256 132ee81328c72ea80949e3d98195f7e23c25379cc616d855d590ea6d2713148e generated by VLA 3e28af1618c86ec8c5b6a20b1aa68f2390fde2d7. The new entry has its own commit identity and requires explicit --first-plan-code-sha to distinguish first-prediction provenance from branch execution provenance. Source f7e25ca68f3b59334adabaf9dbdfa23fca4cf644, checkpoint/normalizer, task/epoch, actual query time and physical initialization gates remain identical. The first two points are exactly the saved commands; all original fork pose/q/dq/velocity/clock checks remain at their original tolerances.

Initial RGB is independently rendered from the matched physical condition, not asserted to be a pixel-identical rerender. Persist all four current images in first_condition.json, their SHA256 hashes and per-image absolute pixel differences against the saved first images. Pixel differences are descriptive, not evidence of harmless noise or geometric correctness. RGB-only source camera pose metadata remains stale and is not consumed by this RGB model.

The 1.6-second post-fork window, zero extra hold, trusted joint7 hold, no artificial closure, no-grasp-assist profile with declared base/support locks, source-clock RTT measurement and 0.5% saturation gate remain unchanged. This is paused-physics research, not autonomous complete-task or real-time qualification.

Isaac Kit may return OS exit 0 after a failed summary. The external launch supervisor must record both raw process exit and a semantic validation exit derived from the exactly one episode summary. Preserve old OS exits; never reinterpret failed summary as successful execution.
