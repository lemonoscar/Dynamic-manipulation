# 第 1700 步模型评测：开环完成，完整自主闭环仍阻塞

本轮使用训练停止后保留的 `best.pt`，其 `global_step=1700`，不是第 1700 个 epoch。没有重启训练、修改权重或使用 depth。

## 身份和范围

- 模型仓库：`feat/rgb-full-episode-finetune-20260908@e03cb0bffd1a3bdeabd39ad8e94e1bbfd9a3e532`。
- 权重：`/diff/wallx_workspace/dzb/integration_runs/rgb_full_episode_20260908_v1/trained/best.pt`。
- 权重 SHA256：`474d03ff22da939f763574bf434c75226a2a2533ae984ced80e25e34a1df7fa0`。
- 完整 Qwen 与两个动作专家严格重载；FP32 参数、BF16 前向 autocast；没有 LoRA 或新增 depth 通道。
- 正常化器身份：`eb5b5fa3116b595e0e65539b7b9d482a84882971c5b78d31a9cc8f7b05a10ae1`；核对完整 payload，没有重拟合或替换语义。
- 数据发布 SHA256：`4e5aaff4a65878e873add2d153f447f6e639bbc9537498e66ca98cd2a0856391`。
- 动作 profile：`causal_command_5hz`，首标签为 query 时刻，10 点、间隔 0.2 秒；双相机 RGB 当前/历史相差 0.2 秒。物理控制 50 Hz，0.4 秒重规划。
- 物理执行代码：`feat/full-step1700-evaluation-20260908@29e4dea1cdfc2aaf6755490e95ed7e8c5dd39f6e`；服务启动身份为同分支较早的 `24188ef95a3156af1c50539ada4f14d1d03f2116`，随后只修了应用端运行器，服务/模型实现不变。最终文档提交与这些实际运行身份分开记录。
- H20 源物理版本：`arm-vla-388b681@388b6818f4c605a707d13c519fbb58b1d07acd92`，Isaac Sim 5.1。是从源状态初始化的新条件，**不是 Sim6 求解器精确复现**。
- 外置证据目录：`/diff/wallx_workspace/dzb/integration_runs/full_step1700_evaluation_20260908_v1`。

## 开环：完整 held-out 数据实际采样

先完成全部 validation，再用同一冻结协议完成 test。每个 split 都有 13 个家族，固定随机种子 17/29/43。1022 个 validation 查询产生 3066 个动作块，1094 个 test 查询产生 3282 个动作块；另外各 66 次交接自由生成。共 6348 个动作块和 132 次交接生成。总墙钟 868.825 秒。

合法动作查询使用已提交的 canonical active task 和既有任务历史，因而衡量的是**正确活动任务条件下的动作质量**。交接生成单独测量。普通输入不包含未来标签、评估真值或 depth。失效的跨阶段后缀不作为标签误差，完整 10 点预测仍用于冻结限幅门。

|测试集任务|有效预测前缀平均误差|前 0.4 秒平均误差|夹爪二值状态准确率，块宏平均|
|---|---:|---:|---:|
|NAV_TO_SOURCE|0.05453 m|0.01583 m|—|
|PICK|0.05237 rad|0.02488 rad|94.7%|
|NAV_TO_TARGET|0.02162 m|0.01076 m|—|
|PLACE|0.02226 rad|0.01204 rad|45.5%|

夹爪指标按每个 query×seed 的有效前缀先求准确率，再对块等权平均；不是所有点的微平均。测试集点微平均分别是 PICK 5474/5850=93.57%，PLACE 1570/4170=37.65%。短窗口权重不同，两个口径均保留。PLACE 的错误不是目标都落在 0.5 阈值附近造成的。

validation 限幅事件率 **9.222%**；test **9.388%**，均未通过原有 ≤0.5% 门。没有事后改门。交接 JSON 结构各 66/66 合法，但 validation CONTINUE/ADVANCE 准确率 34/66=51.5%，test 41/66=62.1%。FINISH、REPAIR_SUFFIX 没有本轮合格标签支持，不能据此宣称学会完整 planner。

逐行预测、分层指标、family bootstrap 区间、采样稳定性、哈希及完整冻结协议见 `results/open_loop/`。结构合法、有限数值和严格加载通过，不等于策略能力通过；旧训练 loss 也不替代实际采样指标。

## 物理协议与边界

只使用已授权 H20 GPU2 推理、GPU3 仿真；显式限制 CUDA 和 RTX 都不占其他 GPU。服务仅监听 `127.0.0.1:18170`。没有新采集、没有跨机公开模型端口。

本轮最多 6 次尝试，包括启动失败；每次最多 900 秒，仍受原训练加验证总截止 `2026-09-08 23:12:46 CST` 限制。78 次/14 小时扩展矩阵没有获批，也没有启动。先后预选 N `16100019/t180`、B `16100181/t260`，从真实源 query 的显式 control/observation/sample 引用读取状态；不 zip 行号。

物理 profile 为 `no_grasp_assist`，有声明的底盘/支撑锁；没有抓取固定约束或中途物体重置，**不标为纯物理条件**。任务条件是固定 PICK，不借 teacher phase 或 evaluator 自动推进完整任务。joint7 有效目标负责夹爪命令；旧模拟接口要求 paired joint7/joint8 字段，但不平均被动 joint8 监督。等待沿用本 episode 可信生效目标。

每个物理 tick 检查实际关节位置、速度、有限数值和目标边界；速度上限 3 rad/s。0.2 秒动作点的目标速率限制不能保证实际 50 Hz 物理速度。推理时暂停物理，仅记录各自主机计时，不声称实时通过。

RTC-on 复用该 family 的首次预测，校验源状态和时间；前两点继续执行，随后使用实时 RGB 和真实 VJP。初始 RGB 有轻微渲染差异，字节不相等；共享首次预测和物理前缀相等不等于后续条件无任何渲染噪声。精确像素差和配对检查见独立审核。

物理结果表由关闭后的原始 summary/trace 汇总到 `results/physical/physical_results.json`。启动故障、真实策略失败、严格接触 unknown 分开计数；完成诊断窗口也不等于完整搬运成功。

## 阻塞和后续最小工作

完整自主搬运闭环未完成：当前桥缺独立当前持物/完成反馈、可执行导航的在线几何与停止安全证据，现有转向分支仅报告 turn-required。不能用评估真值补齐这些条件，也不能把固定 PICK 成功替代全任务成功。R、完整自主 episode、严格接触、持续物理推进下的实时实验均未获得通过证据。

后续先修清实际速度超限及 PLACE 夹爪/阶段交接错误，再补部署反馈与合法导航，按独立预算跑完整 episode。当前权重不批准部署，不通过增加尝试数掩盖安全失败；没有自动继续训练或启动重训。

旧物理外层在 episode summary 已落盘后，还尝试打印缺失的 `pure_physics_success` 字段，产生误名为 startup_failure 的附加报告；进程仍返回了 0，具体退出清理原因尚未定位。故本轮判断以真实 episode summary、trace 和进程终止共同为准，不能将退出码 0 当成功，也不能把结束打印故障当成模型未执行。历史记录保持原样。

## 实际物理结果与最终资源状态

共 6 次尝试：01 因系统盘满导致临时 USD 损坏、机器人创建失败；02 因 pinned 模拟器的双夹爪字段要求而停止。两次都没有模型动作应用，均保留且计入尝试预算。01 原内部 summary 停留在 running，但进程已失败退出，不能解释为仍在运行。

03–06 是实际物理执行的 4 条固定 PICK 诊断，来自 2 个 family、各 RTC off/on，全部失败；这不是 4 条完整 episode。

|尝试|新模型推理次数|物理 ticks / 模型驱动 ticks|首个速度越界时刻|arm_joint2 实测速度|限幅率|结论|
|---|---:|---:|---:|---:|---:|---|
|physical_03_N_off|4（另复用 0 次）|91 / 71|1.82 s|3.40750 rad/s|17.143%|速度门失败|
|physical_04_N_rtc|3（另复用 1 次）|91 / 71|1.82 s|5.04861 rad/s|16.429%|速度门失败|
|physical_05_B_off|8（另复用 0 次）|171 / 151|3.42 s|4.12296 rad/s|16.071%|速度门失败|
|physical_06_B_rtc|4（另复用 1 次）|111 / 91|2.22 s|3.38933 rad/s|16.571%|速度门失败|

合计 19 次新模型推理（其中 7 次真实 VJP），另有 2 次共享首次预测采用；464 个真实物理控制 ticks，其中 384 个应用模型命令，其余 80 个用于视觉历史准备。两组配对的首预测和前 40 tick 完全相同，tick41 后分叉。N on 越界速度更大；B on 比 off 更早触发速度门。没有观察到可信 pick/carry/release 或 geometry hold，不支持 RTC 改善本轮执行，也不能推广为 RTC 在所有条件都无效。

严格接触未知，4 条都没有通过部署安全门。本轮是每 0.4 秒重规划的短诊断，未完成“旧首块继续 / 独立替换 / VJP RTC”三组、统一 1.6 秒主窗口及额外保持的完整 V2 矩阵。安全门后的轨迹不补造，也不补成功。推理暂停模式无法评价迟到、真实网络负载下队列耗尽或实时任务完成率。

本轮 6 次物理尝试墙钟合计 1002.922 秒；停止在预算内，未追加尝试。

资源清理核实时间：**2026-09-08T21:48:49.938238+08:00**。本轮所有模拟进程与推理 PID824950 均已退出；GPU2/3 无计算进程，127.0.0.1:18170 已关闭，本轮 tmux 已结束。其他用户 GPU0/1 进程保持运行。训练未重启。
