# Step1700 训练集 demo 搜索（2026-09-09）

用户授权：最多新增24条、总墙钟6小时；H20 GPU2/3。沿用每条60秒仿真任务时钟，加载不计入单条时钟；批次墙钟从2026-09-09 00:08:57+08:00保守计入准备/等待，06:08:57后不启动新实例，已启动者正常跑完。无自动重试，不重启训练，不增加depth。先让既有003/004/006批次关闭并释放本任务资源。

权重仍为完整模型第1700优化步，SHA256 `474d03ff22da939f763574bf434c75226a2a2533ae984ced80e25e34a1df7fa0`，normalizer `eb5b5fa3116b595e0e65539b7b9d482a84882971c5b78d31a9cc8f7b05a10ae1`，causal_command_5hz、10点、RGB四帧、RTC off。新Sim5.1源t80状态初始化，不是Sim6求解器复现。

## 最小接口修复

协议v2单独保留高层原始生成文本、合法性与错误；非法转换的proposal为null，绝不改成猜测CONTINUE或推进任务。已生成的合法低层动作继续当前canonical task，仍执行身份/时间/数值检查。请求或上下文错误、模型/GPU异常不吞。旧strict函数默认行为保留。

模型使用本family冻结训练视图的original_instruction；原场景task的丰富方向描述保存为独立来源。物理任务仅移除3个旧metadata键并替换资产绑定路径，其余原始字段不变。新版本解决输入文字不一致及错误耦合，不代表修复了模型过早ADVANCE的策略能力。

## 候选选择

在真实train family中，要求四路合法动作、源教师完整事件及t80仍为NAV_TO_SOURCE；排除已测16100000/003/004/006。按t80到源目标XY距离、源NAV总时长、seed排序，预先冻结首24。用离线教师事件筛选，绝不向在线planner输入未来事件或evaluator truth。N/B/R表示来源族；本次从其t80状态开始，不注入源轨迹后来发生的扰动/恢复。

全部候选均有第一epoch987步完成日志与冻结确定性调度的消费证据；这是调度重建，不是直接逐行训练日志。搜索是训练集best-of示例，不证明泛化或成功率。

## Demo候选判据（见新结果前冻结）

必须60秒/3000连续tick正常关闭，逐tick真实物体tensor，模型请求响应身份匹配；有模型NAV_TO_SOURCE→PICK转换；旧pick物理门和既有relative-grasp-evidence-v2稳定几何门通过。独立再检查至少1秒连续物体抬升≥0.04m、TCP距物体≤0.08m、实测joint7≤0.02m；无附着约束/中途物体reset，视频300帧关闭。

候选出现后批次停止新增，下载完整视频、trace、任务/命令/manifest、summary、审核和哈希，人工检查画面后确认demo。严格接触缺少传感证据仍为unknown；既有manipulation底盘/支撑关节锁定明确保留。导航到抓取并稳定抬起是本次搜索目标，完整搬运/放置另列实际结果。

达到24条或墙钟预算无候选就保留失败并报告，不无限补齐。不改当前运行代码，不覆盖旧批次。
