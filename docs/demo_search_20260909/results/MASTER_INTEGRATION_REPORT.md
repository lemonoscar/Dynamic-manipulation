# Step1700 训练集完整推理搜索：最终报告

2026-09-09，北京时间。结论：24/24 条物理尝试正常跑满 60 秒，没有找到完成任务切换且稳定抓起可乐的合格 demo。尝试预算耗尽后停止，没有新增第25条，也没有重训。完整搬运成功 0/24；严格接触和严格完整成功均 unknown，不能当成通过。

## 执行与版本

- 冻结代码：Dynamic-manipulation，feat/train-demo-search-20260909，3af61fa800d09dbfee126dc1ea7b3abaa54bf8fb。结束后工作树仍干净。本报告在独立文档工作树提交，不改变运行身份。
- 第1700优化步权重（不是1700轮）：474d03ff22da939f763574bf434c75226a2a2533ae984ced80e25e34a1df7fa0；normalizer：eb5b5fa3116b595e0e65539b7b9d482a84882971c5b78d31a9cc8f7b05a10ae1。
- RGB四帧，causal_command_5hz，10点，RTC off，模型推理时暂停仿真。未改权重、未加depth；不能据此证明RTC收益或实时能力。
- 新Sim5.1源t80状态初始化，起始任务NAV_TO_SOURCE；不是Sim6求解器恢复。教师原有辅助与学生无抓取辅助存在差异；本次没有物体附着/中途reset，保留并声明底盘及支撑锁定。
- GPU2负责模型，GPU3串行物理；预算最多24条、6小时，从00:08:57保守计入准备等待。24条结束于05:17:01，模型服务05:17:03退出，总预算计时约5小时8分钟，未到06:08:57墙钟上限。每条仅在60秒仿真任务时钟正常结束。
- 运行目录：/diff/wallx_workspace/dzb/integration_runs/train_demo_search_20260909_v1。原监督PID999876、模型PID1005287、末条物理PID1328590已于05:21现场核实退出；GPU2/3没有残留本任务计算进程。不会自动恢复搜索。

## 实际结果

24条均有3000连续物理tick、3000次真实物体tensor读取、完整60秒/300帧视频和可核验响应身份，共72000 tick、3599次请求/响应。24份summary的SHA256在本地逐份重新核对；选中录像对应的完整trace在本地重新运行审核，与远端关键结果完全一致。其余23条未在本地重跑完整trace审核，远端审核与摘要保留。

22/24 条出现模型NAV_TO_SOURCE→PICK转换，16条进一步声明NAV_TO_TARGET，1条声明PLACE；这些是模型声明，不能计作实际导航完成或抓取成功。全部实际pick、稳定几何持有及独立连续1秒抓取门均未通过。最高物体正向抬升仅6.154毫米，低于冻结40毫米要求。

高层非法转换124次均保留错误记录，v2接口不再因此丢弃同一响应已生成的合法低层动作。此接口修复生效，但模型仍会在未抓起时推进任务，修复接口不能代替学习任务衔接。

原0.5% saturation门保持不变：24条均未获通过，其中20条有可计算数值，范围10.238%–24.286%；另4条数值为null，不把缺失当作数值超限或通过。部署验收均未通过，诊断没有因此提前终止。

## 本地录像：seed16100021（失败示例）

按预先写入下载器的稳定样本数、最大抬升、PICK最近距离顺序选择；所有稳定样本数均为0，因此此例由最大抬升选出。它不是成功demo，也不是全体最小TCP距离的实例。

9.2秒模型切入PICK；手臂靠近可乐但未稳定夹住。21.0秒模型又声明NAV_TO_TARGET。PICK期间TCP最近80.688毫米，最大抬升6.154毫米，稳定持有0秒；完整任务失败。已查看5秒间隔概览及9.2/12/15/18/21秒中选取的关键图像（实际重点查看15、18秒原分辨率帧），罐体随后倒在台面，夹爪离开时没有持物。视频与轨迹一致；没有把接近、碰倒或任务声明当作抓取。视频为60秒、1280×720、5fps、300帧；记录有9次overview缺图警告，但最终文件可完整解码。

## 解释范围与下一步

这是对已训练family的预选best-of搜索，有明显选择偏差，不是留出集成功率。初始化状态来自训练family，但新的求解器状态及随后模型自身观察偏离教师轨迹，不能等同于逐帧训练数据重放。结果说明当前step1700在本协议下尚未形成可用的导航—抓取衔接；不能仅凭本实验确定是训练步数、动作拟合、观测分布、教师辅助还是阶段反馈中哪一项单独导致。

最小后续诊断应在16100021/16100026固定实例上分别核对训练原图动作误差、同刻在线图像/状态差异和推进任务时的证据，再决定是否需要训练或改反馈。本轮未执行新实验、未追加预算。高层过早推进、低层闭合与跟踪、无辅助物理迁移仍未解决。

## 工件与操作

本地完整包及录像已下载并重新核验SHA256。全24条的task/command/attempt/summary/demo_audit/process日志和发布manifest在results下，选中失败实例另含完整trace和视频。其余23条完整trace与视频仍保留在远端，未删除。

- 视频SHA256：e8dcf55be4db47176b8070f0314752e7eedacd45ee614fd32e134764a704b41e。
- 包SHA256：436b88a7a6055839ef88e9e525a750b88bfdc003d894042dbe349026a1bdd2bb，30,095,503字节。
- validation_matrix.json：全部24条指标、门、错误和工件哈希。
- DOWNLOAD_RECEIPT.json：绝对本地路径及解码验证。
- LOCAL_REAUDIT.json：所选完整trace本地复核。
- results/search_complete.json、results/service_stop.json：结束原因与本任务服务退出。

下载器两次因临时SSH断连退出，主控确认连接恢复后只恢复同一下载，旧错误归档；没有重启物理实验。交付后暂停本任务自动跟进。没有运行中的本批次可停止；再次测试须另行确定新预算与新输出目录，不能自动再跑24条。
