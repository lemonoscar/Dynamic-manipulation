# RGB-D master integration — 2026-09-08

This branch extends `feat/vla-rolling-rtc-20260907@bf78b041131b05d801a643f2cbe89fda12a3361a` in the separate `ConveyorVLA-master-rgbd-20260908` worktree. Original data, weights and normalizer remain immutable.

`collection_importer.CollectionEpisode` now consumes the actual collection release actuator schema in place. It joins samples by effective command identity, verifies every control tick has its camera sample, rejects duplicate observation identities, extracts named q6/dq6, uses only the actuated joint7 target / 0.04 for command supervision, and preserves calibrated measured finger states without clipping. Per-channel held parents remain separate. Commands outside the exact physical range quarantine all overlapping real intervals with a dedicated range reason, independently of valid command provenance. Declared physical assistance is retained; `bc_eligible=false` perturbations are isolated. Recovery events change offline attempt/epoch identity even when the primitive stays PICK.

NAV labels explicitly remain future measured references. Teacher phases provide offline action route identities, never ordinary planner inputs. Task views return no labels while source task queries remain UNRESOLVED. The source events and their file hashes remain available for future causal labeling; same-simulation-time events must also pass wall-clock availability before entering a pre-observation query.

Depth comes from samples' dual-camera contract 1.1, `isaac_dynamic_z_plus_static_mesh_raycast_v1`, not the stale observations.depth flag. Effective camera K, live camera pose and same-time base pose form each camera-to-base matrix. PNG uint16 millimeters use scale 0.001 and an in-memory `depth > 0` mask. New geometry explicitly uses pixel-center offset 0.5; legacy projection defaults to zero. RGB order remains front previous/current, wrist previous/current. RGB/3DGS alignment is still source-declared unvalidated and is not certified by successful decoding.

`prepare_staged_data.py` can create an explicit bounded development-only release with train-family-only normalization. `cache_staged_conditions.py` consumes true RGB and both PNG depths; it reports each cached query. Candidate training/reload entry points remain independent from the original checkpoint. The authorized smoke is 30 steps, effective batch 2, GPU2 only, at most 45 minutes. No formal retraining is authorized.

The rolling Isaac adapter now requires a same-episode effective-target witness to hold or begin carrying NAV. It no longer changes gripper preload to measured aperture; missing initialization evidence is rejected. A separate physical safety-stop callback is exposed, and complete NAV carrying validation remains pending; the isolated PICK physical diagnostic below exercises trusted-target holds.

Validation artifacts are external: `/diff/wallx_workspace/dzb/integration_runs/master_rgbd_20260908_b/`. The complete real N pilot contains 1945 controls, 1945 camera bundles and 7780 decoded RGB/depth files. Action views: 1390 eligible (40 NAV_TO_SOURCE, 209 PICK, 877 NAV_TO_TARGET, 264 PLACE); 297 queries cross task boundaries, 100 cover the out-of-range joint7 target at tick393, and 99 lack the complete real 2-second application interval. NAV reachability verification phases retain their NAV identity. Planner rows: zero. These counts are data-consumption evidence, not policy success, depth efficacy or a production collection release gate.

The first GPU cache attempt at 7863f15 produced one real RGB-D condition and then failed because a runtime locale change made the next task JSON read ASCII. Its artifacts remain in smoke_v1. Cache JSON reads now explicitly use UTF-8; retry uses a new directory and an immutable new commit. Complete application intervals are checked separately from label points, so causal decimation cannot hide the final hold interval or synthesize an episode tail.

The completed `smoke_v2` at `91d72dd` cached 24 real RGB-D queries, trained the independent RGBD-C0 candidate for 30 steps (all finite losses), saved `model.pt`, strictly reloaded it in the separate inference process, and decoded a 10×7 proposal to absolute joints and joint7 meters. Exit 0 at 2026-09-08 00:36:32+08:00. The new normalizer is `1bbd4eae006613f352452f8c1116c3c28c72c486b33b4434c0eb281cd8092c5b`. Artifacts and complete hashes are in `smoke_v2/validation_report.json`; this is a data/training plumbing smoke, not a useful policy.

Real-RGB VJP numerics at `91d72dd` used the original checkpoint and normalizer, canonical PICK, and three **new N** query pairs separated by 0.4s. They are not reproductions of archived 000006/000024/000030 physics. All 3 RTC-off outputs matched the legacy DiT bitwise; all 6 directional finite differences passed the preregistered tolerances. Four-step VJP cost 0.062–0.084s versus about 0.032s independent sampling, excluding Qwen encoding. No parameter gradients accumulated. The report is `rtc_rgb_v2/report.json`; this numerical run has no physical trajectories; later bounded physical evidence is reported below. The failed first external runner's fixed-10-point normalizer shape error is retained in `rtc_rgb_v1.log`.

`serve_conditioned_pick_rtc.py` adds a distinct loopback-only diagnostic protocol. It requires observation/history/epoch identity and legacy timing. RTC receives explicitly available prior absolute targets aligned by actual apply time, verifies episode/task/weight/normalizer identity, masks unavailable prior targets, and uses the existing codec plus old normalizer to re-anchor them. It returns proposals and local service elapsed time; it does not authorize execution. Regular `policy.predict` also no longer references an undefined `rtc_contexts` variable. The old standalone conditioned runner still has a measured-hold implementation and is not an approved path for the new physical RTC experiment.

The RTC HTTP protocol was actually exercised at `c3b45a7` on GPU3: two real-image POST requests returned 200, and a mismatched task epoch returned 400. Same-host client RTTs were 0.813/0.236s; server elapsed times 0.810/0.233s. These are not cross-machine latency measurements. The original saturation gate **failed** (0.17143/0.11429 versus the unchanged 0.005 limit); no physical command was executed. The dedicated server was stopped by its owning test driver, and its final KeyboardInterrupt is an intentional shutdown. Final CPU regression at this implementation: **656 passed, 2 skipped**, 16.148s. The two original skips remain external locomotion weights and explicitly disabled CUDA alignment. Consolidated identities and artifact hashes are in `vla_master_rgbd_validation_20260908.json`.

## Sim6 RTC physical preparation and failed-B real import

The independent `scripts/run_sim6_rtc_pick.py` entry binds an explicit clean Sim6 source SHA and an archive code manifest (commit plus file hashes). It restores real N pre-state with the Sim6 articulation XYZW bridge; the object wrapper stays WXYZ. Initial arm/joint7 targets come from the preceding verified same-reset effective control, never from measured gripper opening. Each later hold reads effective installed targets. It uses no source settling or measured-target warmup before initialization. Source object state is initializer/evaluator only and is absent from HTTP inputs.

Three fresh processes run `old_tail`, `replacement`, and `rtc`. The first branch records one live RGB first prediction; the original protocol required identical initial RGB plus numeric state and shared-first-two-point endpoint checks (1e-5 frozen tolerance). The authorized v2 amendment below retains physical gates and records independent RGB differences. The remaining eight points form the 1.6 s window, with no fabricated tail or extra hold. Replacement/RTC query every two points; prior RTC targets are aligned by actual apply times, reanchored by the existing service codec, and weighted by the existing exponential prefix schedule. Inference pauses physics and separately records client RTT and server elapsed time. This is a new Sim6 condition, not reproduction of old 000006/24/30 and not real-time or autonomous task validation. Existing 0.5% saturation gate stays unchanged.

Source read-only dependency preflight: `/hdd1/dzb_xhq/.conda-envs/arm-vla-isaacsim6/bin/python` is Python 3.12.14; torch/numpy/PIL/scipy/pydantic/yaml/gymnasium/isaaclab/hydra/requests/cv2 are discoverable. Module discovery alone does not prove application launch. At this preparation checkpoint, no B physical process had been launched. Source CPU archive import, Sim6 readback, actual motor targets, first-prefix identity and physical safety remain runtime hard checks. No GPU resource was used by preparation. `request_real_rtc_probe.py` prepares or sends one real N RGB request over a loopback tunnel and measures RTT with one client's perf_counter; it does not execute actions.

The retained failed-B package `/diff/wallx_workspace/dzb/data_releases/master_rgbd_B_failure_20260908/episode` was actually imported and all 2816 RGB/depth assets decoded at unchanged collection importer identity. It contains 704 controls and 1408 depth records; 253 valid local action queries remain (NAV_TO_SOURCE 99, PICK 154), while 99 epoch crossings, 109 intervention windows, 85 gripper range windows and 99 missing-application-tail windows are quarantined. Planner rows remain zero. Whole-episode failure does not discard valid prefixes. Evidence: `integration_runs/master_rgbd_20260908_b/import_validation_B_failure_v1`; golden SHA256 `ee21769eb7948e9f5c1aa8dcd9fdcf6f4c69697f25b7cadfb28003a8db27fad9`; decoded asset-hash manifest digest `3a9499633cf5acc50477d6b840427c8e78a986113c1344f7b1b6f427cb01a968`. This is not full N/B/R success coverage.

C reviewed the final entry: source velocity/quaternion readback, current camera capture/render evidence, unique query/branch identity with explicit first-plan adoption, command-owner sequence IDs, response identity/duration/base/finite checks and per-query saturation results are enforced. Final focused CPU suite: 25 passed. At that preparation checkpoint, actual source startup and paired physical endpoints were still unverified. Subsequent evidence and the explicitly amended RGB-only comparison are below.


The complete real R success package was decoded at the unchanged importer: 2176 controls, source unknown 0, 8704 RGB/PNG assets, 4352 depth records; 1417 action views (178 NAV_TO_SOURCE, 125 PICK, 853 NAV_TO_TARGET, 261 PLACE). Quarantine reasons: 297 epoch, 109 intervention, 195 calibrated-gripper range, 99 actual-application tail. Real retry switches epoch 2 to 3 at tick457 and retains explicit pick_retry_1 identity. Planner remains ineligible. The retained golden is 151989df1db19c9c6149f6bf5bea5fdab5dc35d935f8a157c1c6b26cdfc6cb41; source-side comparison is separate.

Source physical setup exposed three integration failures, all retained: namespace-package collision before physics; the direct pipeline rejects batch-only --no-color; and the first actual Sim6 launch restored state successfully but failed while reading Warp PhysX limits. The device-bound launcher is now a real subclass, preserving the source CUDA/Vulkan bridge (CPU execution of the actual f7 helper produced physics_gpu0/render_gpu4). Whole Warp arrays are converted with wp.to_torch before row slicing, including limits and target readback; actual source Warp CPU conversion passed. The entry now parses the final downstream source CLI during preflight and checks episode summary even if the source raises SystemExit(0). Seven focused CPU tests passed. These fixes do not count as completed model-action physical trajectories; runtime measurements remain with the master source experiment artifacts.


## Completed bounded physical RTC comparison

Source f7e25ca68f3b59334adabaf9dbdfa23fca4cf644 and the new N220 initialization were used throughout. The service stayed at cc6fdb54069cea4bce46a0b83c55269455aee7b9 with the original 5 Hz checkpoint/normalizer, four sampling steps, one H20 GPU3 and loopback-only authorized tunnels. VLA 3e28af1618c86ec8c5b6a20b1aa68f2390fde2d7 executed the old-tail baseline and generated first_plan SHA256 132ee81328c72ea80949e3d98195f7e23c25379cc616d855d590ea6d2713148e. Two v4 comparison processes were rejected by initial JPEG hash inequality despite exactly matching source/model identities and first-query physical states; their summaries and OS exits remain unchanged.

Master authorized exactly two further attempts under `v2_N220_independent_render_protocol_20260908.md`. VLA 51a4941ffcf06f984e784d8bd215bc31355aa121 explicitly bound the adopted first-plan code SHA, preserved source/model/normalizer/task/time identity and every physical tolerance, and persisted actual initial four-image payloads, hashes and pixel differences. Initial image MAEs were 8.74–10.77/255, so these are independently rendered conditions, not pixel-identical visual reproductions; the differences are not assumed to be harmless noise. Pure RGB source camera pose metadata used a stale Sim6 sensor cache; this legacy RGB model does not consume it. Depth-enabled collection uses the separately implemented live pose path. No source recorder was changed for this diagnosis.

Three actual model trajectories completed: old tail, independent replacement, true VJP RTC. Each applied the same first two commands for 0.4 seconds followed by an identical-duration 1.6-second scoring window, without extra hold. First-query and fork q/dq/root pose/velocity/object pose/velocity/step/time matched **exactly** across all three runs. Replacement and RTC each made four true-image cross-machine requests and applied their decoded outputs; their response rtc_applied flags were all false and all true, respectively. The service serialized requests. Physics paused during inference, so network/service costs remain visible and there is no real-time claim.

| Metric, old tail / replacement / RTC | Observed |
| --- | --- |
| Main-window joint tracking mean absolute error (rad) | 0.01514 / 0.01521 / 0.01520 |
| Final TCP-to-object distance (m), geometry only | 0.05215 / 0.01178 / 0.00951 |
| Final measured gripper fraction | 1.0 / 1.0 / 1.0 |
| Pick verified / geometry hold proxy | false for all / false for all |
| Strict contact grasp | unknown for all |
| Original 0.5% saturation gate | failed for all |
| Source-clock HTTP RTT (s) | 0.755; 0.344–0.435; 0.400–0.840 |

The RTC geometry distance does not establish superior policy effectiveness. This is one development condition, all branches failed to grasp, image pixels differ, strict contact coverage is incomplete, and physical base/support locks remain declared (`pure_physics=false`, no grasp constraint created). Queue exhaustion, asynchronous lateness/cancellation and complete autonomous task behavior were not exercised by this paused 1.6-second experiment. No full-task or deployment pass is claimed.

Source v4/v5 summaries, full request/action traces, actual initial RGB, initial/fork comparisons, process identities and SHA256 manifests are retained in `rtc_physical_N220_source_evidence_v1` under the external B artifact root; they are evaluator evidence, not training input. Kit close can yield OS exit 0 despite failed summary. The v5 external supervisor records both raw process exit and semantic summary-derived exit; the two rejected v4 summaries stay failed. The source processes were closed and GPU4 released; the dedicated H20 service is stopped by its owning bounded supervisor after this comparison.

## Actual prompt-only high-level inspection

A separate GPU2 run used existing Qwen3-VL-4B-Instruct on 30 audited real causal N queries, with four actual RGB images and only query-visible fields. Frozen input SHA256 d7525f2ef9438accb731a0c925effdf1af0998a08a456f82309dc3b9d851e040, prompt SHA256 6886b4741350e38d506d41d5eb1ec8b826d6c6993e7d823efd6945b58ff33a21. All queries lacked reliable planner memory and deployment feedback; the prompt explicitly required UNRESOLVED in that case. No teacher phase, evaluator truth or offline labels entered the prompt. Inference took 286.785 seconds with zero optimization steps; GPU2 was released.

All 30 responses passed the fixed output schema and retained structured unknowns. B and independent C review found three unsupported global-history assertions in reasons (004/006/007 claimed no earlier interaction despite only a 0.4-second visible action history). Report 30/30 structure separately from 3/30 unsupported reason assertions. This is prompt-instructed formatting/uncertainty inspection, not H0/H1/H2 comparison or evidence for CONTINUE/ADVANCE/REPAIR_SUFFIX/FINISH capability. No planner training dataset was fabricated. Responses SHA256 ec7b3278aea72f924706656905e8dca243dd0b458bed11a9ce32c775d46d7458; complete reports in `v3_prompt_only_v1`.

Collection release remains an independent Master decision. This importer consumes source depth contract 1.1; it does not automatically certify later depth 1.2 candidates or their background geometry. B cannot replace missing trusted B teacher success, source depth geometry acceptance or an explicitly authorized new production budget with successful model I/O. Full high-level adaptation, depth-model benefit, trained 25 Hz policy, complete autonomous episodes and deployment safety remain unestablished.
